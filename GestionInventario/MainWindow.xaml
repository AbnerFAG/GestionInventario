<Window x:Class="GestionInventario.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:GestionInventario"
        mc:Ignorable="d"
        Title="Buscar Productos" Height="450" Width="800"
        WindowStartupLocation="CenterScreen">

    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Barra de búsqueda -->
        <StackPanel Orientation="Horizontal" Margin="0,0,0,10">
            <Label Content="Nombre del producto:" VerticalAlignment="Center"/>
            <TextBox x:Name="txtNombreProducto" Width="200" Margin="10,0"/>
            <Button Content="Buscar" Width="100" Click="BtnBuscar_Click"/>
        </StackPanel>

        <!-- Resultados -->
        <DataGrid x:Name="dgProductos"
                  Grid.Row="1"
                  AutoGenerateColumns="True"
                  IsReadOnly="True"
                  CanUserAddRows="False"/>
    </Grid>
</Window>
